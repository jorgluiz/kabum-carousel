import React from 'react'
import { StyledFooter } from '../../styles/styled-components/layout/Footer'

const Footer: React.FC = () => {
  return <StyledFooter></StyledFooter>
}

export default Footer
