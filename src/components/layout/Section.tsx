import React from 'react'
import { StyledSection } from '../../styles/styled-components/layout/SectionStyles'
import Carousel from '../carousel'

const Section: React.FC = () => {
  return (
    <StyledSection>
      <Carousel />
    </StyledSection>
  )
}

export default Section
